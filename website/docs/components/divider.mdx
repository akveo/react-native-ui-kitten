---
id: divider
title: Divider
sidebar_label: Divider
description: Divider is a thin line component that groups content in lists and layouts.
keywords:
  - React Native
  - UI Kitten
  - Divider
  - separator
---

# Divider

Divider renders a thin horizontal line used to separate content in lists and layouts. It automatically uses the Eva theme border color.

## Import

```tsx
import { Divider } from '@ui-kitten/components';
```

## Basic Usage

```tsx
import React from 'react';
import { Layout, Text, Divider } from '@ui-kitten/components';

const DividerExample = () => (
  <Layout style={{ padding: 16 }}>
    <Text category="p1">Section One</Text>
    <Divider />
    <Text category="p1">Section Two</Text>
  </Layout>
);
```

## Props

| Property | Type | Default | Description |
|----------|------|---------|-------------|
| `style` | `ViewStyle` | - | Additional styles applied to the divider line. |
| `appearance` | `'default'` | `'default'` | Appearance defined in the Eva mapping. |

Divider also accepts all standard React Native `View` props.

## Storybook

Live examples coming soon via Storybook.
