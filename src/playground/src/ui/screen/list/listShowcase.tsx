import React from 'react';
import {
  ListProps,
  List,
  ListElement,
} from '@kitten/ui';

export const ListShowcase = (props?: ListProps): ListElement => {
  return (
    <List {...props}/>
  );
};
