import React from 'react';
import {
  Layout,
  LayoutElement,
  LayoutProps,
} from '@kitten/ui';

export const LayoutShowcase = (props?: LayoutProps): LayoutElement => {
  return (
    <Layout {...props}/>
  );
};
