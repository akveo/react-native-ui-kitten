import React from 'react';
import {
  Layout,
  LayoutProps,
} from '@kitten/ui';

export const LayoutShowcase = (props?: LayoutProps): React.ReactElement<LayoutProps> => {
  return (
    <Layout {...props}/>
  );
};
