export const RkTabBarIndicatorTypes = (theme) => ({
  _base: {
    container: {
      height: 4,
    },
    content: {
      flex: 1,
      borderRadius: 2,
      backgroundColor: theme.colors.success,
    },
  },
});
