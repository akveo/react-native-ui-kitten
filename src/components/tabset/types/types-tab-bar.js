export const RkTabBarTypes = (theme) => ({
  _base: {
    container: {
      base: {
        flex: 1,
        flexDirection: 'row',
        justifyContent: 'space-around',
      },
      scrollable: {
        flexDirection: 'row',
        justifyContent: 'flex-start',
      },
    },
  },
});
